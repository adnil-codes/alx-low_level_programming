#include <stdio.h>

/**
 * main - task
 *
 * Return: Alphabets in lower and uppercase
 * with a new line
 */
int main(void)
{
char ch = 97;
while (ch < 122)
{
putchar(ch);
ch++
}
char ch = 65;
while (up < 90)
{
putchar(ch);
ch++
}
putchar('\n')
return (0);
}

