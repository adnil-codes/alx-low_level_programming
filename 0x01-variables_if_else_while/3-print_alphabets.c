#include <stdio.h>

/**
 * main - task
 *
 * Return: Alphabets in lower and uppercase
 * with a new line
 */
int main(void)
{
char c = 65;
while (c < 90)
{
putchar(c);
c++
}
char c = 97;
while (c < 122)
{
putchar(c);
c++
}
putchar('\n');

return (0);
}

