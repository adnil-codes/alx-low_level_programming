#include <stdio.h>

/**
 * main - task
 *
 * Return: 0 when codes ru
 */
int main(void)
{
char ch = 97;
for (ch = 97; ch < 122; ch++)
{
putchar(ch);
}
char up = 65;
for up = 65; up < 90; up++)
{
putchar(up);
}

return (0);
}

