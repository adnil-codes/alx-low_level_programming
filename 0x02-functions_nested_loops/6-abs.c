#include "main.h"
#include <stdlib.h>

/**
 * _abs - absolute
 * @r: integer of code
 * Description: This should print the absolute value
 * Return: always 0
 */
int _abs(int r)
{
r = abs(r);
print("%d", r);
return (0);
}
