#include "main.h"
#include <stdlib.h>

/**
 * _abs - absolute
 * @n: integer of code
 * Description: This should print the absolute value
 * Return: always 0
 */
int _abs(int n)
{
if (n < 0)
{
n *= -1;
return (n);
}
else
{
return (n);
}
}
