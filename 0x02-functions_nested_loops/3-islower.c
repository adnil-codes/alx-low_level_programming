#include "main.h"

/**
 * _islower - this code checks for lower case
 * @c: is the parameter
 * Return: 1 if success
 */
int _islower(int c)
{
if (c >= 97 && c < 122)
{
return (1);
}
else
{
return (0);
}
}
